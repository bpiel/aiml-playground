(ns flojure-tens.scratch
  (:require [flojure-tens.thing2 :as t])
  (:import tfnative.Graph))

#_ (140294167222768
    (def gg (Graph.))

    (def h1 (.doAllocate gg))
    
    (.getOp (Graph.) h1 "h00000000")

    (def tfg (org.tensorflow.Graph.))

    (.ref tfg)

    )
